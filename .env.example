# Copy this to .env and fill in your key.
# .env is gitignored — never commit your real key.
OPENAI_API_KEY=sk-your-key-here

# Optional model override (default is reasonable):
# OPENAI_CHAT_MODEL=gpt-4o-mini
